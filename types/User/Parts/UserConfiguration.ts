export const DEFAULT_INSTANT_EARN_OFFER_LIMIT = 3_000;

type UserConfiguration = {
  instantEarnOfferLimit: number,
  dailyInstantWithdrawalLimit: number,
  maxAffiliateCodes: number,
};

export default UserConfiguration;
